{
  "entities": {
    "Student": {
      "title": "Student",
      "type": "object",
      "description": "Represents a student's data imported from an XLSX file.",
      "properties": {
        "data": {
          "type": "object",
          "description": "A map of key-value pairs representing the student's information from the spreadsheet.",
          "additionalProperties": {
            "type": "string"
          }
        }
      },
      "required": [
        "data"
      ]
    }
  },
  "auth": {
    "providers": [
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}/students/{studentId}",
        "definition": {
          "entityName": "Student",
          "schema": {
            "$ref": "#/backend/entities/Student"
          },
          "description": "Stores student data uploaded by a specific user.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user."
            },
            {
              "name": "studentId",
              "description": "The unique identifier of the student."
            }
          ]
        }
      }
    ]
  }
}