
{
  "entities": {
    "User": {
      "title": "User",
      "type": "object",
      "description": "Represents a user in the system, which can be an administrator, teacher, or student.",
      "properties": {
        "uid": {
          "type": "string",
          "description": "The unique identifier from Firebase Authentication for auth users, or a generated ID for student users."
        },
        "name": {
          "type": "string",
          "description": "The user's full name."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address. Can be a placeholder for students."
        },
        "profileId": {
          "type": "string",
          "description": "The ID of the profile assigned to this user (e.g., 'Administrador', 'Aluno')."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "photoURL": {
          "type": "string",
          "format": "uri"
        },
        "rm": {
          "type": "string",
          "description": "Registro do Aluno (Student ID), used as a unique key for students."
        },
        "ensino": { "type": "string" },
        "serie": { "type": "string" },
        "classe": { "type": "string" },
        "turno": { "type": "string" },
        "filiacao_1": { "type": "string" },
        "filiacao_2": { "type": "string" },
        "rg": { "type": "string" },
        "nis": { "type": "string" },
        "id_censo": { "type": "string" },
        "cpf_aluno": { "type": "string" },
        "cpffiliacao1": { "type": "string" },
        "data_nascimento": { "type": "string", "format": "date" },
        "endereco": { "type": "string" },
        "telefones": { "type": "array", "items": { "type": "string" } },
        "transporte_escolar": { "type": "boolean" },
        "carteira_estudante": { "type": "boolean" },
        "nee": { "type": ["string", "null"] },
        "boletim": { "$ref": "#/entities/Boletim" },
        "status": {
            "type": "string",
            "enum": ["ATIVO", "NÃO LISTADO"],
            "description": "Status of the student record."
        },
        "position": {
            "type": "string",
            "description": "The user's job title or position (e.g., 'Professor de Matemática')."
        },
        "phoneNumber": {
            "type": "string",
            "description": "The user's contact phone number."
        },
        "dateOfBirth": {
            "type": "string",
            "format": "date",
            "description": "The user's date of birth."
        },
        "bio": {
            "type": "string",
            "description": "A short biography or professional summary."
        },
        "profileCompleted": {
            "type": "boolean",
            "description": "Flag indicating if the user has completed their initial profile setup."
        }
      },
      "required": ["uid", "name", "email", "profileId"]
    },
    "Profile": {
      "title": "Profile",
      "type": "object",
      "description": "Defines a user role and its set of default permissions.",
      "properties": {
        "id": { "type": "string" },
        "name": {
          "type": "string",
          "description": "The name of the profile (e.g., Administrador, Professor)."
        },
        "description": {
          "type": "string",
          "description": "A brief description of the profile's responsibilities."
        },
        "color": {
            "type": "string",
            "description": "A hex color code to associate with the profile for UI elements."
        },
        "permissions": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
                "view:students",
                "manage:students",
                "view:users",
                "manage:users",
                "view:profiles",
                "manage:profiles",
                "view:grades",
                "manage:grades",
                "view:attendance",
                "manage:attendance",
                "view:announcements",
                "manage:announcements",
                "view:database",
                "manage:database"
            ]
          },
          "description": "A list of permission keys granted to this profile."
        }
      },
      "required": ["id", "name", "permissions"]
    },
    "Grade": {
      "title": "Grade",
      "type": "object",
      "description": "Represents a student's grade in a specific subject.",
      "properties": {
        "id": { "type": "string" },
        "studentId": { "type": "string" },
        "classId": { "type": "string" },
        "subject": { "type": "string" },
        "grade": { "type": "number" },
        "term": { "type": "string", "description": "The grading period (e.g., '1st Trimester')." }
      },
      "required": ["id", "studentId", "classId", "subject", "grade", "term"]
    },
    "Attendance": {
      "title": "Attendance",
      "type": "object",
      "description": "Represents a student's attendance on a specific date.",
      "properties": {
        "id": { "type": "string" },
        "studentId": { "type": "string" },
        "classId": { "type": "string" },
        "date": { "type": "string", "format": "date" },
        "status": {
          "type": "string",
          "enum": ["Presente", "Ausente", "Justificado"]
        }
      },
      "required": ["id", "studentId", "classId", "date", "status"]
    },
    "Announcement": {
      "title": "Announcement",
      "type": "object",
      "description": "Represents a school-wide or class-specific announcement.",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "content": { "type": "string" },
        "targetAudience": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Array of profile IDs or class IDs to target."
        },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "title", "content", "targetAudience", "createdAt"]
    },
    "Nota": {
      "title": "Nota",
      "type": "object",
      "description": "Armazena as notas de um aluno para uma disciplina específica ao longo do ano.",
      "properties": {
        "etapa1": { "type": ["number", "null"], "description": "Nota da Etapa 1." },
        "etapa2": { "type": ["number", "null"], "description": "Nota da Etapa 2." },
        "etapa3": { "type": ["number", "null"], "description": "Nota da Etapa 3." },
        "etapa4": { "type": ["number", "null"], "description": "Nota da Etapa 4." },
        "mediaFinal": { "type": ["number", "null"], "description": "Média final da disciplina." }
      }
    },
    "Boletim": {
      "title": "Boletim",
      "type": "object",
      "description": "Um mapa de disciplinas e suas respectivas notas para um aluno.",
      "additionalProperties": {
        "$ref": "#/entities/Nota"
      }
    }
  },
  "auth": {
    "providers": ["password", "google.com"]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": { "schema": { "$ref": "#/entities/User" } }
      },
      {
        "path": "/profiles/{profileId}",
        "definition": { "schema": { "$ref": "#/entities/Profile" } }
      },
      {
        "path": "/grades/{gradeId}",
        "definition": { "schema": { "$ref": "#/entities/Grade" } }
      },
      {
        "path": "/attendance/{attendanceId}",
        "definition": { "schema": { "$ref": "#/entities/Attendance" } }
      },
      {
        "path": "/announcements/{announcementId}",
        "definition": { "schema": { "$ref": "#/entities/Announcement" } }
      }
    ]
  }
}
